var React = require('react');

var Stopwatch = React.createClass({
    render: function () {
        return (
            <div>
                <h1>
                    Stopwatch page
                </h1>
            </div>
        );
    }
});

module.exports = Stopwatch;